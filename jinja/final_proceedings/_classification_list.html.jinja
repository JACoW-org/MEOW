+++
title = """[{{classifications|length}}]"""
+++

{% set prev_classification_group = None %}

<section>
  <div class="classifications">

    {% for classification in classifications %}

    {% if classification.track_group %}
    {% set classification_group = classification.track_group.title %}
    {% else %}
    {% set classification_group = None %}
    {% endif %}

    <div class="classification-group">
      <b>{{ classification_group }}</b>
    </div>

    <div class="classification-item">
      <a data-href="classification/{{ classification.code|lower }}/index.html" 
        onclick="openUrl(`classification/{{ classification.code|lower }}/index.html`)" 
        href="javascript:void(0);"><b>{{ classification.title }}</b></a>
    </div>

    {% endfor %}
  </div>
</section>